# Vercel Postgres — otomatis terisi saat menautkan database di dashboard Vercel.
# Untuk pengembangan lokal, salin ke .env.local lalu isi dari
# Vercel → Storage → Postgres → .env.local tab.
POSTGRES_URL="postgres://user:password@host:5432/database?sslmode=require"
